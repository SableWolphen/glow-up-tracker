<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/colorPrimaryDark</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <!-- windowActionBar/windowNoTitle here were supposed to be enough on
             their own, but a real device still showed a persistent native
             black title bar ("PlushLife", from title_activity_main) above
             the app's own content — even with a code-level
             getSupportActionBar()?.hide() added too. The likely reason:
             this theme was never hooked up with postSplashScreenTheme, so
             the activity stays on the raw Theme.SplashScreen ancestry for
             its entire lifetime instead of the documented core-splashscreen
             pattern of handing off to a real app theme once the splash
             exits — which on the platform's own Android 12+ splash-screen
             integration is exactly the scenario where "no action bar"
             doesn't reliably stick. AppTheme.NoActionBar above already has
             the right Theme.AppCompat.DayNight.NoActionBar ancestry for
             that handoff; it just needed to actually be wired in. -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
        <!-- android:background used to point at @drawable/splash here, but
             that's a generic legacy attribute the platform SplashScreen
             system doesn't manage — with this theme staying applied to the
             activity's window after launch (see below), a sliver of that
             drawable could keep rendering under edge-to-edge instead of
             being properly dismissed, showing up as a stray squished icon
             at the top of the screen. windowSplashScreenBackground/
             windowSplashScreenAnimatedIcon are the attributes the splash
             system actually manages, and installSplashScreen() in
             MainActivity is what tells it to dismiss cleanly. -->
        <item name="android:windowSplashScreenBackground">@color/colorPrimary</item>
        <item name="android:windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <!-- Shows only during the brief splash window now that
             postSplashScreenTheme hands off to AppTheme.NoActionBar
             afterward — pinned to a plain color (no night variant) so it's
             never dark regardless of system dark mode. -->
        <item name="android:windowBackground">@color/colorPrimaryDark</item>
    </style>
</resources>