<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <!-- android:background used to point at @drawable/splash here, but
             that's a generic legacy attribute the platform SplashScreen
             system doesn't manage — with this theme staying applied to the
             activity's window after launch (see below), a sliver of that
             drawable could keep rendering under edge-to-edge instead of
             being properly dismissed, showing up as a stray squished icon
             at the top of the screen. windowSplashScreenBackground/
             windowSplashScreenAnimatedIcon are the attributes the splash
             system actually manages, and installSplashScreen() in
             MainActivity is what tells it to dismiss cleanly. -->
        <item name="android:windowSplashScreenBackground">@color/colorPrimary</item>
        <item name="android:windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <!-- This theme still stays applied to the activity's window after
             the splash itself is gone, so windowBackground is what shows
             through the gap edge-to-edge leaves at the top of the screen.
             Pinning it to a plain color (no night variant) keeps that gap
             the app's own pink regardless of system dark mode. -->
        <item name="android:windowBackground">@color/colorPrimaryDark</item>
    </style>
</resources>